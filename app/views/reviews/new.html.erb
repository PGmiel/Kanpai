<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, as: :select, collection: [0, 1, 2, 3, 4, 5] %>
  <%= f.submit "Submit review", class: "btn btn-dark" %>
<% end %>

<%= link_to 'Back', restaurants_path %>